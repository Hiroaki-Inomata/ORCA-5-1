W93 {
  title varchar(256);
  popup_summary varchar(256);
  popup_body varchar(1024);
  popup_icon varchar(256);
  popup_timeout int;
  fixed1 {
    RECEDEN_COMB {
      RECEDEN {
        textdata varchar(20);
      };
      count int;
        item varchar(20)[10];
    };
    JMAMR_COMB {
      JMAMR {
        textdata varchar(20);
      };
      count int;
        item varchar(20)[10];
    };
  };
};
