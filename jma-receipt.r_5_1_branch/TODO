日医標準レセプトソフト TODO list
================================
* CPCOMMONSHELL*.INC に依存しているものを CPHOMEDIR.INC を使用するように
  書き換える。(CPCOMMONSHELL*.INC は廃止する)。

* サブディレクトリを変更可能にする。
  CPHOMEDIR.INC に(もしくは新しいCOPY句を作成して) SCRIPTS, SCREEN, 
  FORM, RECORD....を追加し、そこから取得するように変更する。

* site-upgrade で etc をコピーしないようにする。
